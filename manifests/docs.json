{"v":0,"docs":{"introduction--docs":{"id":"introduction--docs","name":"Docs","path":"./src/stories/introduction.mdx","title":"Introduction","content":"import { Meta } from \"@storybook/addon-docs/blocks\";\n\n<Meta title=\"Introduction\" />\n\n{/*\n  The prototype is a sibling of this Storybook: deployed it sits at /prototype/ on the\n  same GitHub Pages site, and locally it's a separate dev server on :3000. Resolve the\n  base at render time so the links below work in both places rather than only one.\n*/}\n\nexport const protoBase = () => {\n  if (typeof window === \"undefined\") return \"https://jg-tenfore.github.io/tf-fox-ds-v1/prototype\";\n  const { origin, pathname } = window.location;\n  // Deployed (or previewed from a static build): the prototype is a sibling of this\n  // Storybook under the same base path, whatever that base happens to be.\n  const i = pathname.indexOf(\"/iframe.html\");\n  const base = i > 0 ? pathname.slice(0, i + 1) : \"/\";\n  if (base !== \"/\") return `${origin}${base}prototype`;\n  // Storybook dev server at the domain root: the prototype is its own dev server.\n  return \"http://localhost:3000\";\n};\n\nexport const Proto = ({ path = \"/\", children }) => (\n  <a href={`${protoBase()}${path}`} target=\"_blank\" rel=\"noreferrer\" style={{ fontWeight: 600 }}>\n    {children}\n  </a>\n);\n\nexport const TABS = [\n  [\"Home\", \"/\", \"Quick-book across all nine courses, the course portfolio with green fees, the Academy\"],\n  [\"Tee Times\", \"/tee-times/\", \"All nine sheets intertwined into one board sorted by the clock, course pills, a resident-rate switch, per-course rate card → checkout → confirmation\"],\n  [\"Shop\", \"/shop/\", \"A 53-SKU county pro shop (25 of them MCG crest merchandise) → product → cart → pickup-at-course checkout\"],\n  [\"Events\", \"/events/\", \"27 county events — leagues, the County Amateur, junior tour qualifiers, charity scrambles\"],\n  [\"Instruction\", \"/instruction/\", \"The MCG Academy — 23 real instructors across nine courses; pick a pro, pick their lesson, then time, details and payment\"],\n  [\"Calendar\", \"/calendar/\", \"One month view across all nine courses — events, clinics and Academy programs\"],\n  [\"Clinics\", \"/clinics/\", \"14 county clinic programs, filtered by level, age and course\"],\n  [\"Grill\", \"/grill/\", \"Course grills and snack bars, with order-ahead for the turn\"],\n  [\"Sign up\", \"/signup/\", \"Sign up → verify → account, plus sign in and password reset\"],\n  [\"Account\", \"/account/\", \"Profile hub, activity history, lesson-credit wallet, settings\"],\n];\n\nexport const TabTable = () => (\n  <table style={{ width: \"100%\", borderCollapse: \"collapse\", margin: \"16px 0 0\" }}>\n    <thead>\n      <tr>\n        <th style={{ textAlign: \"left\", padding: \"8px 12px 8px 0\", borderBottom: \"1px solid #e4e7ec\", fontSize: 13, color: \"#667085\", width: 140 }}>Tab</th>\n        <th style={{ textAlign: \"left\", padding: \"8px 0\", borderBottom: \"1px solid #e4e7ec\", fontSize: 13, color: \"#667085\" }}>What's there</th>\n      </tr>\n    </thead>\n    <tbody>\n      {TABS.map(([label, path, detail]) => (\n        <tr key={path}>\n          <td style={{ padding: \"10px 12px 10px 0\", borderBottom: \"1px solid #f2f4f7\", verticalAlign: \"top\", whiteSpace: \"nowrap\" }}>\n            <Proto path={path}>{label} ↗</Proto>\n          </td>\n          <td style={{ padding: \"10px 0\", borderBottom: \"1px solid #f2f4f7\", fontSize: 14, lineHeight: 1.55, color: \"#475467\" }}>{detail}</td>\n        </tr>\n      ))}\n    </tbody>\n  </table>\n);\n\n<p style={{ fontSize: 13, fontWeight: 700, letterSpacing: \"0.14em\", textTransform: \"uppercase\", color: \"#667085\", margin: \"0 0 6px\" }}>\n  Tenfore Golf\n</p>\n\n# Fox Design System\n\n<p style={{ fontSize: 20, lineHeight: 1.55, color: \"#475467\", maxWidth: 680, margin: \"6px 0 0\" }}>\n  The component library, booking product, and multi-club theming system behind Tenfore's golf\n  experiences — built on Untitled UI, Tailwind CSS v4, and React Aria, and documented end-to-end here in Storybook.\n</p>\n\n<p style={{ fontSize: 14, color: \"#98a2b3\", margin: \"18px 0 0\" }}>\n  562 stories &nbsp;·&nbsp; 13 categories &nbsp;·&nbsp; 4 club colorways &nbsp;·&nbsp; WCAG AA\n</p>\n\n<div style={{ display: \"flex\", gap: 16, flexWrap: \"wrap\", margin: \"28px 0 0\" }}>\n  <div style={{ flex: \"1 1 320px\", border: \"1px solid #e4e7ec\", borderRadius: 12, padding: \"18px 20px\", background: \"#fff\" }}>\n    <p style={{ fontSize: 12, fontWeight: 700, letterSpacing: \"0.1em\", textTransform: \"uppercase\", color: \"#667085\", margin: 0 }}>\n      You are here\n    </p>\n    <p style={{ fontSize: 17, fontWeight: 600, color: \"#101828\", margin: \"8px 0 4px\" }}>Storybook — the library</p>\n    <p style={{ fontSize: 14, lineHeight: 1.5, color: \"#475467\", margin: 0 }}>\n      Every component and screen in isolation, with all of its states. The place to review and validate a change.\n    </p>\n  </div>\n  <div style={{ flex: \"1 1 320px\", border: \"1px solid #1e8e4e\", borderRadius: 12, padding: \"18px 20px\", background: \"#f6fef9\" }}>\n    <p style={{ fontSize: 12, fontWeight: 700, letterSpacing: \"0.1em\", textTransform: \"uppercase\", color: \"#1e8e4e\", margin: 0 }}>\n      New\n    </p>\n    <p style={{ fontSize: 17, fontWeight: 600, color: \"#101828\", margin: \"8px 0 4px\" }}>\n      <Proto path=\"/\">MCG Prototype — the product ↗</Proto>\n    </p>\n    <p style={{ fontSize: 14, lineHeight: 1.5, color: \"#475467\", margin: 0 }}>\n      A clickable seven-tab booking experience for Montgomery County Golf. Real URLs, real navigation, and a session\n      that carries a cart and a booking history across the whole app.\n    </p>\n  </div>\n</div>\n\n## The MCG Prototype\n\n**Montgomery County Golf** is a nine-course public county system in Maryland — Falls Road, Northwest,\nHampshire Greens, Laytonsville, Little Bennett, Needwood, The Crossvines, Rattlewood, and Sligo Creek. The prototype is the whole\nproduct assembled around it, and it's the artifact to put in front of a stakeholder when a Storybook story\nisn't enough.\n\nEvery tab in the global nav works. Browsing is open — nothing is gated — and **any credentials sign you in**.\n\n<TabTable />\n\n### State that carries\n\nThe prototype keeps one object in `localStorage`, so a walkthrough behaves like an application rather than a\nslide deck:\n\n- **Add to cart** → the nav badge and total update, and the cart survives a refresh\n- **Book a tee time or a lesson** → it shows up in **Account → Activity**\n- **Buy a lesson package** → credits land in **Account → Wallet** and can be spent at lesson checkout\n- **Set county residency** anywhere → every course reprices to the resident rate\n- **Account → Settings → Reset prototype data** clears it all for the next demo\n\n### Why it can't drift from this Storybook\n\nBoth are built from the same source tree — there is no copy step between them. Each screen's UI lives in\n**one file** under `src/components/`, and neither the route nor the story contains any markup of its own:\n\n```\nsrc/components/mcg/tee-times/tee-sheet-screen.tsx     ← the screen (one copy)\n        │\n        ├── src/app/tee-times/page.tsx                ← the route: a thin wrapper\n        └── src/stories/mcg/tee-times.stories.tsx     ← the story: renders the same component\n```\n\nValidate a change in either place and the other already has it. The **MCG Prototype** category in the\nsidebar is those same screens, story by story.\n\nTwo details make it work: `asset()` resolves brand imagery for both hosts (Storybook serves images at the\nsite root, the prototype from a nested base path), and `useSession()` returns an inert session when no\nprovider is present, so a component that reads the cart renders unchanged inside a story.\n\n## The map\n\n| Category | Stories | What's inside |\n| --- | --- | --- |\n| **Foundations** | 45 | Design tokens & primitives — Colors, Typography, Spacing, Radius, Border, Effect Styles, Icons, Logos, and Golf Courses brand data. |\n| **MCG Prototype** | 54 | Every screen in the prototype above, rendered from the same components the routes use. |\n| **Instruction** | 32 | The MCG Academy — lesson catalog, instructor selection, facility calendar, waitlists, and three package pricing models with a comparison page. |\n| **Components** | 255 | The Untitled UI core and application kit, in semantic sub-folders — Actions, Forms, Navigation, Feedback & Status, Layout & Structure, Media & Visuals. |\n| **Design Systems** | 70 | Colorways, color-theory combinations, and per-club style guides (Kettle Hills, Sagamore, FloGolf). |\n| **Booking** | 31 | The tee-time booking engine — molecules (search, calendars, slot cards) and full pages. |\n| **Global Nav** | 27 | The Sagamore/Kettle-Hills product surfaces — Tee Times, Pro Shop, Events, Calendar, Clinics, Restaurant. |\n| **Profile ∕ Account** | 15 | Overview, Activity, Wallet, Memberships, Golf Buddies, My Account. |\n| **Explorations** | 13 | Alternate concepts & integrations — GolfNow, Google, SevenRooms, and combined flows. |\n| **Sign in ∕ Sign up** | 9 | Authentication screens — log in, sign up, verification, forgot password. |\n| **Tee Time Checkout · Shop Checkout** | 10 | The earlier per-club checkout and confirmation flows. |\n\n## The Instruction category\n\n**Instruction** is the MCG Academy, and the most recent product work. It answers a live question: whether\nTenfore can replace CoachNow for a 25-instructor county academy. Two flows, built to be argued over:\n\n- **Book a Lesson** — service-first, so privates and multi-week clinics share one catalog and one checkout.\n  \"Any available instructor\" is a first-class choice rather than a fallback, which is the fix for landing on\n  one pro's empty calendar and starting over.\n- **Packages & Credits** — three pricing models side by side (a credit book, a volume discount, a monthly\n  plan), with a **Model Comparison** page scoring them across ten dimensions.\n\nPricing is one academy menu plus a per-instructor adjustment, not a catalog per pro — see\n**Instruction → Elements → Pricing Model** for the argument in one table.\n\n## Multi-club theming & Design Systems\n\nA single helper, `clubBrandStyle(color)`, re-points the brand namespace tokens so an entire screen adopts a\nclub's color with no per-component edits. The **Design Systems** category showcases this:\n\n- **Colorways** — the full booking style guide rendered in each hue of the utility palette.\n- **Color Theory** — analogous, complementary, and triadic harmonies derived from real OKLCH hue.\n- **Kettle Hills · Sagamore · FloGolf · MCG** — the live per-club colorways (blue, near-black, green, green).\n- **Accessibility** — every brand surface uses the `-700` shade and clears **WCAG AA (4.5:1)**; each guide badges its worst-case contrast.\n\nOne known limit: `clubBrandStyle` re-points the mid and solid brand tokens but not the light ones\n(`bg-brand-primary` / `bg-brand-secondary` resolve through `--color-brand-50/100` at `:root`), so a\nre-skinned club still shows the default ramp's light tints behind selected states.\n\n## Foundations & components\n\n**Foundations** documents the tokens everything is built from — the semantic color system (`text-primary`,\n`bg-brand-solid`, `fg-success-primary`, …), the type scale, spacing, radius, and the brand logos for each\ngolf course. **Components** is the Untitled UI library those tokens dress.\n\n## How it's built\n\n- **React 19** + **TypeScript** throughout\n- **Tailwind CSS v4** with a semantic, light/dark-aware token system (`src/styles/theme.css`, `palette.css`)\n- **React Aria Components** as the accessibility foundation (imported as `Aria*`)\n- **Storybook 10** (`@storybook/nextjs-vite`) for documentation and visual testing\n- **Next.js 16** static export for the prototype — both deploy to the same GitHub Pages site in one build\n\n### Running it locally\n\n```bash\nnpm run storybook   # this library      → http://localhost:6017\nnpm run dev         # the MCG prototype → http://localhost:3000\nnpm run build:pages # both, combined exactly as CI publishes them\n```\n\n### Conventions\n\n- Files are **kebab-case**; React Aria imports are prefixed **`Aria*`**.\n- Style with **semantic tokens** (`text-secondary`, `bg-primary`) — never literal color classes.\n- Screens live in `src/components/`; routes and stories are thin wrappers that render them.\n"}}}