import { InstructionHubScreen } from "@/components/mcg-2/instruction/instruction-hub";

/** `/instruction` — lessons, clinics and programs in one place (Prototype 2). */
export default function Page() {
    return <InstructionHubScreen />;
}
