import { TeeSheetScreen } from "@/components/mcg-2/tee-times/tee-sheet-screen";

/** `/tee-times` — the county tee sheet. All markup lives in the shared screen component. */
export default function TeeTimesPage() {
    return <TeeSheetScreen />;
}
