import { TeeConfirmationScreen } from "@/components/mcg-3/tee-times/tee-confirmation-screen";

/** `/tee-times/confirmation` — the receipt, read back from the booking checkout just wrote. */
export default function TeeConfirmationPage() {
    return <TeeConfirmationScreen />;
}
