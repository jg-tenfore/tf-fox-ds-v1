import { InstructionHubScreen } from "@/components/mcg-3/instruction/instruction-hub";

/** `/instruction` — lessons, clinics and programs in one place (Prototype 3). */
export default function Page() {
    return <InstructionHubScreen />;
}
