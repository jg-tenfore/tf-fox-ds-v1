import { CheckoutScreen } from "@/components/mcg-3/shop/checkout-screen";

/** `/cart/checkout` — contact, pickup course, card, pay. */
export default function CheckoutPage() {
    return <CheckoutScreen />;
}
