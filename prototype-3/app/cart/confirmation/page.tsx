import { ConfirmationScreen } from "@/components/mcg-3/shop/confirmation-screen";

/** `/cart/confirmation` — the pickup receipt. */
export default function ConfirmationPage() {
    return <ConfirmationScreen />;
}
