import { ShopScreen } from "@/components/mcg-3/shop/shop-screen";

/** `/shop` — the Pro Shop storefront. The screen component is shared with Storybook. */
export default function ShopPage() {
    return <ShopScreen />;
}
